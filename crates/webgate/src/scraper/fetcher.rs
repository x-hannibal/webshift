//! Concurrent HTTP fetcher with streaming, size cap, UA rotation, and retry backoff.
//!
//! Port of `../mcp-webgate/src/mcp_webgate/scraper/fetcher.py`.

// TODO(M1): implement fetch_urls, _fetch_single with streaming cap
