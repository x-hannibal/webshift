//! MCP server entry point: tool registration and stdio transport.
//!
//! Registers three tools: webgate_onboarding, webgate_fetch, webgate_query.
//! Binary name: `mcp-webgate` (configured in Cargo.toml).

fn main() {
    println!("mcp-webgate: not yet implemented — see PLAN.md milestone M2");
}
